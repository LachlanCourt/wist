# WIST

This is a digital version of the card game Wist

## Setup

Setup local certificates `dotnet dev-certs https --export-path ~/devcert.pfx --format Pfx --password your_password`
and import into chrome's certificate trust settings
